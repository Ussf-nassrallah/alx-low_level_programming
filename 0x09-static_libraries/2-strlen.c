#include <string.h>

/**
 * _strlen - function that return the length of string
 * @s: represent string
 * Return: the length of string
 */

int _strlen(char *s)
{
	int strSize = strlen(s);

	return (strSize);
}
