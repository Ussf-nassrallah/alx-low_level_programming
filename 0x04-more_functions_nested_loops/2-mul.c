#include "main.h"
#include <stdio.h>

/**
 * mul - function that multiplies two integers
 * @a: represent first number
 * @b: represent second number
 * Return: result of (a * b)
 */

int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
