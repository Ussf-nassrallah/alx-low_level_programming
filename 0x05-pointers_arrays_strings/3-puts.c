#include "main.h"
#include <stdio.h>

/**
 * _puts - function that prints a string followed by a new line
 * @str: represent the string will be printed
 * Return: ...
 */

void _puts(char *str)
{
	puts(str);
}
