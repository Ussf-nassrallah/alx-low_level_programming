#include "main.h"
#include <stdio.h>

/**
 * main - Entry Point
 * @argc: arguments count
 * @argv: arguments victor
 * Return: 0 Always success
*/

int main(int argc, char **argv)
{
	/**
	 * (void) argv - pay no attention to argv.
	*/
	(void) argv;

	printf("%d\n", argc);

	return (0);
}
